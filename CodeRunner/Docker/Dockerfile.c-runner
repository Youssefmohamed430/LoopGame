FROM alpine:3.20

# Install GCC and C runtime headers
RUN apk add --no-cache gcc musl-dev

# Create non-root execution user
RUN adduser -D -u 1000 sandboxuser

# Create workspace directory
WORKDIR /workspace
RUN chown -R sandboxuser:sandboxuser /workspace

USER sandboxuser
